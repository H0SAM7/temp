import 'package:flutter/material.dart';

class FavoritesView extends StatelessWidget {
  const FavoritesView({super.key});
static String id='FavoritesView';
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}